import readline from "readline";
import fs from "fs/promises";

export function checker() {

                const command = ["check", "exit"] 
                if(command[0]){
                    console.log("hello")
                }/* else if (command === 1){

                }*/

};
